import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mauapp.maucontacttracing',
  appName: 'MauContactTracing',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
